{% extends 'base.html.twig' %}

{% block title %}
	{{ article.title }}
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<button type="button" class="btn btn-outline-info ">
			<a class="text-dark text-decoration-none" href="{{ path('category_item', { id: article.category.id }) }}">Revenir à {{ article.category.name|capitalize }}</a>
		</button>
		<h1>{{ article.title }}</h1>
		<h3>{{ article.category.name|capitalize }}</h3>
        <h6> {{ article.dateCreated|date('d-m-Y') }}</h6>
		<p>{{ article.content }}</p>
	</div>
{% endblock %}
